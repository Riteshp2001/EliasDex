/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.myanimelist.net', 'thumbnail.komiku.org', 'images.unsplash.com', 'myanimelist.net'], // jika gambar dari MAL
  },
};

export default nextConfig;